-- src/agent_engine.lua
agent_engine = {}

function agent_engine.handle_prompt(prompt)
    -- Stub for prompt processing
    print("Agent received: " .. prompt)
    return "success"
end

return agent_engine
